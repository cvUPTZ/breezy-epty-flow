import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

/**
 * @component AspectRatio
 * @description A component that constrains its children to a specific aspect ratio.
 * It's a direct re-export of the `Root` component from `@radix-ui/react-aspect-ratio`.
 */
const AspectRatio = AspectRatioPrimitive.Root

export { AspectRatio }
